<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Meuscompromissos" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header class="bg-stone-800 text-white h-18">
      <div class="container mx-auto p-4 flex justify-between items-center" style="max-width: 1224px;">
        <section id="top" class="w-full flex justify-between items-center">
          <h2 class="sr-only">Homepage</h2>
          <h1><b>MEUS COMPROMISSOS</b></h1>
          <span id="notification" class="flex items-center mx-4">
            <%= lucide_icon('triangle-alert', class: 'text-yellow-300 mr-2') %>
            Você tem 3 horas livres no seu dia
          </span>
          <a href="/profile" id="avatar-profile" class="flex items-center justify-center">
            <p class="mr-2 text-end">
              <b>Elton Santos</b>
              <br />
              <span class="text-gray-30">Ver perfil</span>
            </p>
            <img src="https://github.com/eltonsantos.png" class="h-12 w-12 rounded-full" />
          </a>
        </section>
      </div>
    </header>

    <section id="summary" class="bg-stone-800 text-white">
      <div class="container mx-auto p-4 flex justify-between border-t border-stone-700" style="max-width: 1224px;">
        <h2 class="sr-only">Sumário</h2>
        <div class="grid grid-cols-3 gap-4">
          <div class="bg-gray p-4">
            <strong>5</strong>
            Tarefas ao total
          </div>
          <div class="bg-gray p-4">
            <strong>2</strong>
            Em andamento
          </div>
          <div class="bg-gray p-4">
            <strong>3</strong>
            Encerradas
          </div>
        </div>
        <a class="button orange mt-3 inline-flex items-center bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600" href="/job">
          <span class="mr-2">
            <%= lucide_icon('square-plus', class: 'text-white mr-2') %>
          </span>
          Adicionar novo compromisso
        </a>
      </div>
    </section>

    <main class="mx-auto p-4 flex justify-between items-center" style="max-width: 1224px;">
      <%= yield %>
    </main>

  </body>
</html>
